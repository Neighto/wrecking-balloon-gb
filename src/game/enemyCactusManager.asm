SECTION "enemy cactus manager", ROMX

SpawnEnemyf::
    ; Check if we can spawn an enemy
    ; ^ if respawn timer has looped AND if we have an enemy dead or unspawned
    ; if can spawn enemy1 // if can spawn enemy2

    ret 

